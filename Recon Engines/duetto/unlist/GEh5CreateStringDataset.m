% GEh5CreateStringDataset(outFilePath, groupAbsPath, dataSetName, charArray)
%
% Create a HDF5 string dataset given the group absolute path, dataset name, and string (char array).
%
% Inputs:
%   outFilePath  - HDF5 file where the string dataset is created and written.
%   groupAbsPath - absolute path of group
%   dataSetName  - name of the string dataset
%   charArray    - the string data
